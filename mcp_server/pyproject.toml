[project]
name = "odoo-mca-mcp-server"
version = "1.0.0"
description = "MCP server for Odoo Module Composition Analysis"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.0",
    "requests>=2.31",
]
